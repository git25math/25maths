# Bilingual Revamp Issues And Fixes

## Issue Policy

1. Record every blocker or bug before fixing.
2. Classify severity:
   - P0: release blocker
   - P1: major functional risk
   - P2: medium impact
   - P3: minor
3. Include reproducible steps and closure validation.

## Ledger

| ID | Date | Severity | Type | Description | Root Cause | Fix | Validation | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BR-000 | 2026-02-18 | P2 | Process | Repository has many unrelated in-flight changes from parallel threads. | Shared workspace active multi-thread edits. | Created isolated task tree and constrained current execution to new docs + planned scope files only. | Verified new docs created under `tasks/bilingual-revamp/` with no edits to unrelated files in this step. | Closed |
| BR-001 | 2026-02-18 | P3 | Tooling | `apply_patch` failed when appending Phase-1 CSS helpers to generated `site.css`. | `assets/css/site.css` is a minified single-line output, so line-based patch anchor could not be matched safely. | Switched to controlled append via shell redirection and re-validated output. | Confirmed helper rules present in `assets/css/site.css` and full `jekyll build` succeeded. | Closed |
| BR-002 | 2026-02-18 | P3 | Tooling | Initial `bundle exec jekyll build` runs showed ambiguous long-running session state during `Generating...`, delaying closeout verification. | Build output stream did not emit completion promptly in first run and sandbox blocked `ps` inspection (`operation not permitted`). | Switched to the known stable build command with explicit `BUNDLE_PATH=vendor/bundle` and completed blocking wait for terminal completion line. | Build completed with `done in 51.137 seconds` and rendered `_site` pages include all Phase-3 fields/links. | Closed |
| BR-003 | 2026-02-19 | P2 | SEO | Legacy language route migration relied on static redirect pages (`redirect_to`) rather than platform-level HTTP 301 responses. | Jekyll static hosting behavior serves redirect HTML (meta refresh/script) with canonical, not guaranteed server 301 status. | Added Cloudflare-compatible edge redirect strategy and completed production activation path via Cloudflare Redirect Rules. | Live verification now passes: `/en/`, `/zh-cn/`, `/zh-cn/cie0580/products/algebra.html` all return HTTP 301 with expected `Location`. | Closed |
| BR-004 | 2026-02-19 | P3 | Build Config | Newly added `_redirects` file was not present in build output during first validation pass. | Jekyll excludes underscore-prefixed files by default unless explicitly included. | Added `_redirects` to `include` list in `_config.yml`. | Rebuilt site and confirmed `_site/_redirects` exists with expected rules. | Closed |
| BR-005 | 2026-02-19 | P2 | Infrastructure | Production did not return expected 301 for legacy language paths after Cloudflare cutover. | Cloudflare proxy activation and redirect-rule apply were completed in stages; interim checks observed HTTP 200 before final rule activation. | Applied production redirect ruleset and verified on Cloudflare edge. | `scripts/health/check_redirect_301_live.sh https://www.25maths.com` now returns PASS for all checks with HTTP 301. | Closed |
| BR-006 | 2026-02-19 | P3 | Tooling | Initial version of live 301 check script failed before running checks due malformed `awk` regex. | Regex escape sequence used `^HTTP\\//` instead of `^HTTP\/`. | Corrected `awk` pattern and reran script successfully. | Script now executes full check set and reports expected status (currently fails due BR-005, not script error). | Closed |
| BR-007 | 2026-02-19 | P3 | Ops Enablement | Manual Cloudflare dashboard steps can delay 301 rollout and introduce operator drift. | No automation path existed for redirect-rule apply. | Added API-based apply script `scripts/deploy/apply_cloudflare_redirect_rules.sh` and documented required token scope in playbook. | Script validates required env vars, pushes ruleset payload via Cloudflare API endpoint, and instructs post-apply live 301 check. | Closed |
| BR-008 | 2026-02-19 | P3 | Ops Robustness | Cloudflare API apply failed with `curl: (3) URL rejected: Malformed input to a URL function` in real operator run. | `CF_ZONE_ID` copied from dashboard can include whitespace/newline artifacts, resulting in malformed API endpoint URL. | Updated apply script to trim whitespace from `CF_ZONE_ID`, trim CR/LF from token, and fail early if zone ID is not 32-hex format. | `bash -n scripts/deploy/apply_cloudflare_redirect_rules.sh` passed; script now emits explicit validation error instead of malformed curl URL. | Closed |
| BR-009 | 2026-02-19 | P2 | Security Edge Config | After enabling Cloudflare proxy, live checks temporarily returned `HTTP 403` with `cf-mitigated: challenge`, blocking redirect verification. | Cloudflare security layer (`安全级别`) triggered `托管质询` for test requests. | Added diagnostics + remediation guidance in checker/playbook; operator adjusted security settings to remove challenge state. | `curl -I https://www.25maths.com/en/` now returns `HTTP 200` with `server: cloudflare` and no `cf-mitigated` header. | Closed |
| BR-010 | 2026-02-19 | P2 | Cloudflare Plan Compatibility | API apply for redirect rules failed with HTTP 400 due entitlement error on `regex_replace` in `target_url` expression. | Current Cloudflare plan does not include `regex_replace` support for Redirect Rules target expressions (requires Business/WAF Advanced). | Reworked rules to free-plan compatible implementation: split exact-root and prefix rules; use `substring` to strip `/en` and `/zh-cn` prefixes. Updated script and playbook accordingly. | Operator rerun succeeded (`success: true` in API response), and live 301 checks passed. | Closed |
| BR-011 | 2026-02-19 | P3 | Tooling Compatibility | Apply script reported `success=false` on systems without `rg`, even when API response returned `\"success\": true`. | Success check relied only on ripgrep (`rg`), which is absent in some macOS environments. | Added fallback success detector using `grep -E` when `rg` is unavailable. | Script now supports both environments and no longer emits false failure for successful API apply responses. | Closed |
| BR-012 | 2026-02-19 | P3 | Security Operations | Security hardening guidance was fragmented across troubleshooting responses, making repeatable operations difficult after rollout. | No dedicated baseline playbook/check existed for balancing Cloudflare protection and non-blocking access. | Added `SECURITY-BASELINE-PLAYBOOK.md` and `check_cloudflare_security_baseline.sh`; linked in README and validated against production. | Baseline script passes on production with non-challenged core pages and expected 301 legacy redirects. | Closed |
| BR-013 | 2026-02-19 | P3 | UX Alignment | Bilingual toggle presentation needed to move from static placement to right-edge pull-out floating interaction to match final product direction. | Final UX preference was confirmed after initial toggle-shell implementation; navigation minimalism required non-nav placement. | Implemented pull-out floating panel component and updated styling to edge-docked hover/focus expansion while keeping existing toggle logic. | `bundle exec jekyll build` passed; generated pages contain floating panel markup and nav include contains no bilingual toggle controls. | Closed |
| BR-014 | 2026-02-19 | P1 | Content Coverage | Floating bilingual switch rendered correctly, but several primary pages still displayed English-only after toggle because no bilingual blocks existed on those pages. | Phase-6 delivery completed switch shell behavior first; content coverage on all primary pages was not fully expanded after EN/ZH route consolidation. | Added bilingual markers/content blocks to affected primary pages (`about`, `subscription`, `cie0580 pricing/free`, `edx4ma1 overview/products/pricing/free`, `kahoot`, `blog`). | Playwright interaction checks on local `_site` now show marker counts > 0 and visible Chinese block counts toggling correctly per page. | Closed |
| BR-015 | 2026-02-19 | P2 | UX Behavior | Floating switch needed to be anchored at bottom-right with “shutter” interaction (auto retract to edge, auto expand on right-edge hover proximity). | Previous CSS anchored desktop panel at mid-right (`top: 34vh`) and forced open state when bilingual mode was on. | Reworked panel CSS to bottom-right docking, removed force-open rule, added right-edge trigger strip, and updated switch visuals to explicit on/off icons. | Desktop + mobile Playwright checks confirm `position: fixed`, bottom-right placement, collapsed transform by default, hover expansion, and leave-to-retract behavior. | Closed |
| BR-016 | 2026-02-19 | P2 | UX Behavior | Panel still stayed expanded after toggling ON in some runs, even after mouse left the edge area. | Toggle click left button focus active; combined with panel `:focus-within` expansion rule, the dock remained open. | Added post-toggle `blur()` in `bilingual_support.js` for both click and checkbox-change paths to clear focus lock. | Revalidated with Playwright: when state remains `on`, panel left position returns to collapsed value after pointer leaves (`afterOnLeave == before`). | Closed |
| BR-017 | 2026-02-19 | P1 | Content Coverage | Toggle still appeared ineffective on several module entry routes because those pages still had zero bilingual markers despite floating control being present. | Coverage patch in BR-014 did not include `cie0580` overview, Kahoot board directories, and coming-soon module entry pages. | Added explicit `bilingual-support-only` Chinese guidance blocks on uncovered module pages so ON state always yields visible content changes. | Marker scan confirms nonzero coverage for `cie0580/index`, Kahoot board directories, and coming-soon module pages; build succeeds. | Closed |
| BR-018 | 2026-02-19 | P2 | Deployment Cache | Live sessions could continue rendering stale floating-switch CSS/JS after merge because static asset URLs were unchanged. | Browser/edge cache reused `/assets/css/site.css` and `/assets/js/bilingual_support.js` from previous deployments. | Introduced centralized `assets_version` and versioned CSS/JS includes to force fresh asset fetch after release. | Verified include wiring in `_includes/head.html`, `_includes/mobile-menu-js.html`, and `_layouts/bare.html` resolves to versioned asset URLs. | Closed |
| BR-019 | 2026-02-19 | P1 | CI Style Gate | `check_style_consistency.sh` failed with 11 errors, blocking merge for bilingual rollout branch. | Page-level drift remained on `origin/main`: residual `text-white` paired with `cie/edx-solid-bg`, legacy focus-ring class chain, Edexcel hero not using shared tokenized CTA include, cross-board contamination text in Edexcel free page, amber status surfaces on presale pages, and missing token-chip status badge semantics in `edx4ma1/products.html`. | Refactored affected board pages to board-token semantics (`edx/cie-soft`, `token-chip`), replaced legacy focus chain with `ui-focus-ring`, migrated Edexcel hero CTAs to shared include (`board='edx'`), and removed cross-board contamination strings in Edexcel free module copy. | Local gate now passes: `bash scripts/health/check_style_consistency.sh` => `Failures: 0`; full `bundle exec jekyll build` also passes. | Closed |
| BR-020 | 2026-02-19 | P2 | CI Visual Gate | `check_visual_regression.sh` failed after style-token fixes because baseline snapshots no longer matched updated UI surfaces. | Expected visual drift introduced by intentional token/class updates across CIE/Edexcel/Kahoot/Exercises pages; baseline still reflected pre-fix rendering. | Refreshed only the affected 9 baseline images and revalidated visual diff gate. | `bash scripts/health/check_visual_regression.sh` now passes with all snapshots matched. | Closed |
| BR-021 | 2026-02-19 | P1 | Content Coverage | `/exercises/index.html` remained a high-traffic canonical page where bilingual toggle looked ineffective due to zero marker coverage. | Earlier coverage waves focused on landing/product pages and omitted the interactive exercise hub. | Added bilingual marker blocks to exercise hub header context, resume panel hints, filter labels/help text, live summary line, and empty-state guidance. | `exercises/index.html` marker count is now 12 and toggle ON shows visible Chinese additions in all major hub sections. | Closed |
| BR-022 | 2026-02-19 | P2 | UX Consistency | Exercise hub dynamic summary/resume fallback text stayed English-only while static blocks switched to bilingual mode. | `assets/js/exercise_hub.js` only maintained English template strings and did not target Chinese mirror nodes. | Extended runtime i18n contract (`summaryTemplateZh`, `resumeTitleFallbackZh`, `resumeMetaFallbackZh`) and wired synchronized updates to new Chinese nodes. | `node --check assets/js/exercise_hub.js` passes; runtime now updates EN+ZH summary/resume text together under filter changes. | Closed |
| BR-023 | 2026-02-20 | P1 | Content Coverage | Utility/legal/transaction pages (terms/privacy/thanks/404/membership) lacked bilingual markers, so toggle looked ineffective on those pages. | Prior coverage work prioritized core marketing/module funnels and deferred non-core routes. | Added reusable bilingual note include and injected compact Chinese helper blocks into non-core pages. | `check_bilingual_coverage.sh` confirms these pages now have marker count >0; manual source scan shows no residual zero-marker utility/legal pages. | Closed |
| BR-024 | 2026-02-20 | P2 | Accessibility / UX | Floating bilingual shutter switch lacked explicit switch semantics and had no reduced-motion fallback; bottom-right dock could overlap injected chat widgets. | Initial implementation focused on visual interaction and did not include ARIA switch semantics, motion preference handling, or runtime dock offset compensation. | Added `role=\"switch\"` + `aria-checked` sync, `prefers-reduced-motion` transition suppression, and JS-based dock offset self-heal when chat overlays are detected. | `node --check assets/js/bilingual_support.js` passes; generated markup contains switch semantics; CSS includes reduced-motion and dynamic bottom offset variable. | Closed |
| BR-025 | 2026-02-20 | P2 | Tooling Compatibility | First run of new bilingual coverage guardrail failed on macOS due `mapfile` absence and undercounted include-based bilingual notes as zero markers. | Script assumed bash4-only builtin and marker regex only matched literal classes, not shared include invocations. | Replaced `mapfile` with bash3-compatible `while read` ingestion and expanded marker regex to include `include bilingual/inline-note.html`. | `bash scripts/health/check_bilingual_coverage.sh` now runs on macOS and CI with `Failures: 0`, `Warnings: 0`. | Closed |
