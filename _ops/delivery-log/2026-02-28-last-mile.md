# Last-Mile Member Delivery Log — 2026-02-28

## Round 1: Structural Integrity Checks

### Pre-check | releases.json count
- Status: PASS
- Output: 25 entries (1 free + 24 member)
- Expected: 25

### Pre-check | Missing payhip_product_id
- Status: CONFIRMED BUG → FIXED (Workstream A)
- Output: 22 releases missing → 0 after fix
- Delta: W02-W12 EN + Bilingual had empty payhip_product_id

### Pre-check | Download gateway syntax
- Status: PASS — `node --check` exit 0

### Pre-check | Webhook syntax
- Status: PASS — `node --check` exit 0

### Pre-check | subscription.html eN4l6 link
- Status: PASS — 2 occurrences (href + data-payhip-product)

### Pre-check | Locale subscription variants
- Status: PASS
- `en/subscription.html`: redirect to `/subscription.html` (Jekyll redirect_to)
- `zh-cn/subscription.html`: has `eN4l6` link (line 194-195)

### Pre-check | Locale membership variants
- Status: PASS — No `en/membership/` or `zh-cn/membership/` exist. Single `membership/index.html` only.

---

## Workstream A: Fix releases.json Data

### A1 | Set payhip_product_id for all member releases
- Status: PASS
- Fixed 22/24 member releases: `payhip_product_id: "eN4l6"` + `payhip_url`
- Constraint check: release_id, asset_key, channels unchanged ✓

### A2 | Sync release registry
- Status: PASS
- `node scripts/member/sync_release_registry.js` → 24 `"eN4l6"` in registry

### A3 | Cross-validation (3-way)
- Status: PASS
- JS (member_downloads.js): 24 release_ids
- JSON (releases.json): 24 member release_ids
- Registry (release_registry.js): 25 total entries (24 member + 1 free)
- Missing in any: 0

---

## Workstream B: Member Downloads UI

### B1 | Create member_downloads.js
- Status: PASS
- 130 lines, syntax valid
- Pattern: follows `member_benefits.js` (event-driven via `member-dashboard-data`)
- States: guest → non-member → active (12 week cards)
- Download: `GET /api/v1/download/{release_id}?channel=member` with Bearer token

### B2 | Update membership/index.html
- Status: PASS
- Heading: "This Week's Releases" → "Weekly Practice Packs"
- Grid: `md:grid-cols-3` → `sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4`
- Script: `member_downloads.js` added after `member_center.js`

---

## Round 2: Logic Flow Verification

### Webhook Path (happy path — user already has Supabase account)
1. Payhip `sale_completed` → POST webhook ✓
2. `verifyPayhipSignature` — sha256(PAYHIP_API_KEY) comparison ✓
3. `parsePayhipProductId(payload)` → `"eN4l6"` ✓
4. `isPayhipActivationEvent("sale_completed")` → true ✓
5. `isPayhipSubscriptionEvent("sale_completed")` → false ✓
6. `findReleasesByProductId("eN4l6")` → 24 releases ✓ (FIXED)
7. `effectivePeriodEnd` = 12 weeks from now (one-time purchase) ✓
8. `upsertMembershipStatus` → status: "active", period_end: 12w ✓
9. `upsertEntitlement` × 24 → `expires_at: null` (perpetual) ✓
10. Response: `entitlements_granted: 24` ✓

### Reconcile Path (user purchases before creating account)
1. Webhook → user not found → logged as "pending" ✓
2. User creates account + logs in ✓
3. `member_auth.js` → `triggerMembershipReconcileIfNeeded` ✓
4. `reconcile.js` → `listPayhipEventsForEmail` → finds pending event ✓
5. `applyEventToUser` → grants 24 entitlements ✓
6. Note: `expires_at: effectivePeriodEnd` = null (Payhip sends no period_end for one-time) ✓

### Download Path (member clicks download button)
1. `member_downloads.js` → `handleDownload` ✓
2. `getAccessToken()` from Supabase session ✓
3. `GET /api/v1/download/{release_id}?channel=member` + Bearer ✓
4. `findReleaseById` → found, active ✓
5. `getUserFromAccessToken` → user.id ✓
6. `fetchMembershipStatus` → active ✓
7. `fetchEntitlement` → expires_at: null → valid ✓
8. `requiresActiveMembership` → false (membership_tier is "") ✓
9. Auth: `!strictMembershipRequired && (membershipAllowed || entitlementAllowed)` → pass ✓
10. `isAssetBucketAllowed("member-files", ["member-files"])` → true ✓
11. `createSignedStorageUrl` → signed URL ✓
12. Response: `{ download_url, expires_in }` ✓

### Minor Inconsistency (not a blocker)
- Webhook sets `membership_status.period_end` = 12 weeks (line 176-177)
- Reconcile sets `membership_status.period_end` = null (no 12-week logic)
- Impact: Cosmetic only. Download gateway checks entitlement OR membership. Both paths work.

---

## Deployment Verification

### Git commit + push
- Status: PASS
- Commit: `fb85ac8` on `main`, pushed to origin
- Files: releases.json, release_registry.js, member_downloads.js, membership/index.html, delivery log

### GitHub Pages deployment
- Status: PASS
- `pages-build-deployment` workflow: success

### CI check
- Status: FAIL (pre-existing, unrelated)
- Cause: Legacy focus classes in `thanks.html` + `support-thanks.html`
- Previous commit had same failure — not introduced by our changes

### Live verification
- `member_downloads.js` served: PASS (HTTP 200, correct content)
- `membership/index.html` updated: PASS ("Weekly Practice Packs" heading, 4-col grid)
- Script tag present: PASS (1 occurrence)

### API Functions deployment
- Status: **DEPLOYED** (via Cloudflare Worker proxy)
- Solution: Created Worker `25maths-api-proxy-production` with route `www.25maths.com/api/*`
- Architecture: Worker intercepts `/api/*` requests on `www.25maths.com` → proxies to `25maths-website.pages.dev` (Cloudflare Pages with Functions)
- Worker route ID: `ba210ee969f5446cb3d25f8314b2ee92`
- Worker version ID: `2caccbda-9fe3-4cd2-ac5f-7ee8f96d2cf5`
- Why not DNS change: wrangler OAuth token lacks DNS edit permissions; Worker route achieves same result without DNS migration
- Source: `workers/api-proxy/` in repo

### API endpoint verification (post-Worker deploy)
- `POST /api/v1/membership/webhook/payhip` → `{"error":"Invalid webhook signature."}` HTTP 403 ✓
- `GET /api/v1/download/member-week01-number-fdp-en-2026w10?channel=member` → `{"error":"Authentication required for member download."}` HTTP 401 ✓
- `POST /api/v1/membership/reconcile` → `{"error":"Authentication required."}` HTTP 401 ✓
- All endpoints return proper JSON (not static HTML 404) ✓

---

## Self-Reflection Checkpoints

### CP1: After Round 1 — System state as understood?
- **Assumption confirmed**: 22/24 releases had empty payhip_product_id → fixed
- **Assumption confirmed**: No locale variants of membership page need updating
- **New finding**: API Functions not deployed (static 404) → flagged for Workstream C
- **Missed assumption in plan**: Plan assumed Functions were deployed but just missing env vars. Reality: Functions directory exists but no serverless runtime is configured.

### CP2: After A+B — If user pays now, can they get all 24 files?
- **Code: YES** — releases.json fixed, registry synced, UI built, download gateway logic verified
- **Infrastructure: NO** — API endpoints not live. Webhook cannot receive events. Download gateway cannot serve signed URLs.
- **Dependency**: Workstream C must deploy Functions before any backend API works

---

## Workstream C: Infrastructure

### Updated Checklist

**C0: Deploy API Functions** — ✅ DONE
- Cloudflare Pages project `25maths-website` already existed with all env vars
- Functions work on `25maths-website.pages.dev`
- Created Worker proxy route `www.25maths.com/api/*` → Pages Functions
- All 3 API endpoints verified live (webhook, download, reconcile)

**C1: Get Payhip API Key** — ✅ ALREADY SET (env var `PAYHIP_API_KEY` exists in Pages project)

**C2: Set Cloudflare env vars** — ✅ ALREADY SET
- Confirmed vars: `PAYHIP_API_KEY`, `PAYHIP_WEBHOOK_SECRET`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ALLOWED_MEMBER_BUCKETS`, `DOWNLOAD_SIGN_TTL_SECONDS`

**C3: Configure Payhip webhook** — ❌ PENDING (user action)
- URL: `https://www.25maths.com/api/v1/membership/webhook/payhip`
- Event: `sale_completed` (at minimum)

**C4: Verify product page** — ❌ PENDING (user action)
- URL: `https://payhip.com/b/eN4l6`

---

## Workstream D: E2E Testing
- Status: BLOCKED (waiting for C)
- E2E script ready: `scripts/member/e2e_payhip_flow.sh`
- Test plan: 8 test cases defined in plan (D1-D8)

---

## Verification Summary

| Item | Status |
|------|--------|
| `findReleasesByProductId("eN4l6")` returns 24 | ✅ PASS |
| Member panel shows 12-week download cards | ✅ PASS (code + live HTML) |
| Non-member sees "Get Term Pass" button | ✅ PASS (code) |
| Cross-validation (JS/JSON/registry) | ✅ PASS (0 mismatches) |
| Webhook logic grants 24 entitlements | ✅ PASS (code trace) |
| Download gateway auth logic correct | ✅ PASS (code trace) |
| Live JS file accessible | ✅ PASS (HTTP 200) |
| Live HTML updated | ✅ PASS (curl verified) |
| API Functions live | ✅ PASS (Worker proxy route) |
| Env vars configured | ✅ ALREADY SET |
| Payhip webhook URL set | ❌ PENDING (user) |
| E2E test passing | ❌ BLOCKED (waiting for C3) |
